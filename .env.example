APP_NAME="💂 Odin"
APP_ENV=local
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

# proxy settings: needed if your app is behind a reverse proxy that provides ssl termination
PROXY_SCHEMA= # https
PROXY_URL= # https://odin.yourserver.com

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=odin
DB_USERNAME=root
DB_PASSWORD=secret

QUEUE_CONNECTION=redis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_DRIVER=mailgun
MAIL_FROM_ADDRESS=no-reply@odin.dev
MAIL_FROM_NAME="${APP_NAME}"

ENFORCE_DOMAIN=true
HORIZON_ENABLED=true
DEBUGBAR_ENABLED=false
REGISTRATIONS_ENABLED=true

BROWSERSHOT_NO_SANDBOX=true # set to true if running in docker
